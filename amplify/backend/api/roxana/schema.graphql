# This "input" configures a global authorization rule to enable public access to
# all models in this schema. Learn more about authorization rules here: https://docs.amplify.aws/cli/graphql/authorization-rules
input AMPLIFY { globalAuthRule: AuthRule = { allow: public } } # FOR TESTING ONLY!

type Discussion @model {
  id: ID!
  version: Int!    # == 2
  revision: Int    # incremented on every update
  layout: String!
  layoutStates: [LayoutState!] @hasMany
  users: [User] @manyToMany(relationName: "DiscussionUsers")

  sentences: [Sentence!] @hasMany(indexName: "byDiscussion", fields: ["id"])
  currentSentences: [Sentence!] @hasMany(indexName: "byCurrentDiscussion", fields: ["id"])
}

type LayoutState @model {
  id: ID!
  layout: String!  # immutable
  version: Int!
  discussion: Discussion! @belongsTo
}

type Sentence @model {
  id: ID!
  content: String!  # immutable

  discussionId: ID! @index(name: "byDiscussion")
  discussion: Discussion! @belongsTo(fields: ["discussionId"])
  currentDiscussionId: ID! @index(name: "byCurrentDiscussion")
  currentDiscussion: Discussion! @belongsTo(fields: ["currentDiscussionId"])
}

type User @model {
  username: ID! @primaryKey
  discussions: [Discussion] @manyToMany(relationName: "DiscussionUsers")
}
